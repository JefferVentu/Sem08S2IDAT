const fullName = 'Diego Cueva';
console.log(fullName.trim().length);

// primero los metodos luego las propiedades
// acá cuenta los caracteres sin espacios
// trim: solo elimina espacios al inicio y al final
console.log(fullName.trim().toUpperCase());
//todo mayuscula ↑
console.log(fullName.trim().toLowerCase());
//todo minuscula ↑